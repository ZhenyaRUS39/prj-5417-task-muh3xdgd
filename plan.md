# Plan: prj-5417-task-muh3xdgd

**Project:** `PRJ-8912`  
**Task ID:** `task-muh3xdgd`  
**Repo:** `prj-5417-task-muh3xdgd`  

## Summary

Декомпозиция задачи: парсер INI-файлов с валидацией схемы. 4 этапа — структура, парсинг, валидация, тесты и документация.

## Subtasks

### 1. Создать структуру репозитория

- **ID:** `sub-1`
- **Profile:** `20razrab1`
- **Tester:** `TBD`
- **Status:** `pending`
- **Description:** Создать репо prj-5417-task-muh3xdgd: pyproject.toml (Python 3.11+, ruff, mypy), директории src/ini_parser/, tests/, README.md, .gitignore. Без логики — только каркас проекта.
- **Test plan:** git clone проходит; tree показывает src/ini_parser/__init__.py, tests/, pyproject.toml; pip install -e . не падает

### 2. Реализовать INI-парсер

- **ID:** `sub-2`
- **Profile:** `20razrab1`
- **Tester:** `TBD`
- **Status:** `pending`
- **Description:** Модуль src/ini_parser/parser.py: класс IniParser с методами parse(text) -> dict[str, dict[str, str]], parse_file(path). Поддержка секций [section], ключ=значение, комментариев # и ;, многострочных значений, экранирования. Без валидации — только разбор.
- **Test plan:** pytest tests/test_parser.py зелёный: парсинг простого файла, секций, комментариев, многострочных значений, невалидного ввода бросает IniParseError
- **Dependencies:** `sub-1`

### 3. Добавить валидацию по схеме

- **ID:** `sub-3`
- **Profile:** `30razrab2`
- **Tester:** `TBD`
- **Status:** `pending`
- **Description:** Модуль src/ini_parser/validator.py: класс Schema с правилами required/section/key/type (int/float/bool/str/enum/regex)/range/default. Метод validate(data, schema) -> list[ValidationError]. Коды ошибок: MISSING_SECTION, MISSING_KEY, TYPE_MISMATCH, INVALID_VALUE, EXTRA_KEY (warning). Интеграция в IniParser.parse(file, schema=...).
- **Test plan:** pytest tests/test_validator.py зелёный: required-секция отсутствует, тип не сходится, regex не матчит, range вне границ, default подставляется. Ошибки содержат путь section.key и понятное сообщение
- **Dependencies:** `sub-2`

### 4. Тесты, документация, финальная сборка

- **ID:** `sub-4`
- **Profile:** `30razrab2`
- **Tester:** `TBD`
- **Status:** `pending`
- **Description:** Покрыть парсер и валидатор до ≥85% (pytest-cov). Написать README с примерами: базовый парсинг, парсинг со схемой, обработка ошибок. Запустить ruff, mypy --strict, pytest. Убедиться что CI dry-run (или локально: pip install -e . && pytest) зелёный.
- **Test plan:** pytest --cov=src --cov-report=term-missing показывает ≥85%; ruff check . без ошибок; mypy src --strict без ошибок; README содержит рабочий пример, который запускается из коробки
- **Dependencies:** `sub-3`


---
*Generated by Hermes Orchestrator*
